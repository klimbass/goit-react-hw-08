export const selectModalIsOpen = (state) => state.modal.modalIsOpen;
export const selectModalConfirm = (state) => state.modal.confirm;
export const selectContactId = (state) => state.modal.contactId;
